# Art-Table-App

React + TypeScript app built with Vite. Shows artworks in a paginated PrimeReact DataTable with persistent selection.

## Quick Start

```bash
npm install
npm run dev
```

Open the dev server URL shown in the terminal (default http://localhost:5173).
